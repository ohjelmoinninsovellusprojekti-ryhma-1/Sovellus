
import React from 'react';
import NavigationButton from './navigationnbutton';


const MovieNavigationButton = ({ to, label, icon }) => (
    <NavigationButton to={to} label={label} icon={icon} />
  
);

export default MovieNavigationButton;

